# v1.3 M3b 前端行为对账报告

日期：2026-08-21  
范围：前端结构重构批次；不含页面操作、截图识别、人工视觉验收。

## 本批已落地的边界

| 原职责 | 现入口 | 保持的关键行为 |
| --- | --- | --- |
| 通用 HTTP 会话、错误和下载 | `src/shared/api/http.ts` | 会话头、失效通知、下载和错误翻译只有一个实现 |
| 项目选择、打开、新建、恢复 | `src/features/projectContext/useProjectLifecycle.ts` | 打开后刷新参考数据和最近项目；恢复前仍检查当前底稿是否允许离开；重复恢复由逻辑层阻断 |
| 项目生命周期 API | `src/api/domains/projects.ts` | 路径、请求体、近期项目 URL 编码、健康检查 URL 不变；旧 `api.*` 方法保留兼容 |
| Excel 导入/导出 | `src/features/projectOperations/useExcelTransferOperation.ts` | 导入始终先预检、明确确认后才提交；导出仍经统一下载器 |
| ZIP 归档 | `src/features/projectOperations/useArchiveOperation.ts` | 范围变化即失效预检令牌；无已选单位不能归档；通过预检令牌后才生成 ZIP |

## 自动验证结果

- 后端：Ruff 通过；Pytest `290 passed`（存在第三方 Starlette 的弃用警告）。
- 前端：Lint、TypeScript 类型检查、逻辑测试 `22 passed`、生产构建通过。
- 当前执行机 Node 为 `v26.5.0`，锁定目标为 `v24.19.0`；目标 Node/Windows/macOS 发布验证仍归入 K06。

## 尚未替代人工验收的项目

1. 项目选择器、新建/打开、从 `.auditbak` 恢复的真实桌面交互。
2. 归档、导入、合并、自动备份设置弹窗的视觉布局与键盘焦点。
3. 深浅主题下的长文本、窄窗口和高 DPI 显示。

本报告不把自动构建通过表述为人工 UI 验收通过。
