# 审迹 v1.3 技术栈升级与重构方案

> 制定日期：2026-08-20  
> 基线版本：v1.2.0 / schema v14  
> 适用目录：`audit-trail-main/`  
> 方案性质：技术决策、迁移路线与发布门禁  
> 验证边界：仅使用分层静态扫描、命令行只读检查、自动化测试和官方资料核对；不启动页面，不进行浏览器模拟、页面截图或截图识图。
> 实施步骤计划：`审迹v1.3_实施开发步骤计划表.md`

> **范围调整（2026-08-20，优先于本文其余历史技术建议）**：v1.3 不引入 Tiptap/ProseMirror 富文本编辑器，不开发项目级资料请求台账。相关依赖、数据迁移、性能指标和验收门禁移入 v1.4 候选池；本版本只保留既有兼容数据，不扩展相应工作流。

## 一、结论

审迹需要升级，但不需要推倒重来。

建议采用“保留核心架构、升级运行时、拆分巨石模块、补齐工程门禁、按业务功能渐进迁移”的路线：

- **保留** FastAPI、SQLite、Vue 3、TypeScript、Element Plus、PyInstaller 和本地回环服务架构。
- **升级** Python 3.11 至 Python 3.14 标准 GIL 构建，Node.js 22 至 Node.js 24 LTS；先做兼容性验证，再锁定具体补丁版本。
- **重构** `backend/main.py`、`backend/database.py`、`backend/export.py`、`App.vue`、`api/client.ts`、`ProjectOperations.vue` 等巨型文件，形成按业务域组织的模块化单体。
- **新增** Vue Router、Pinia、排序能力、ESLint、Node 环境 Vitest、OpenAPI 类型生成和依赖安全门禁；Tiptap/ProseMirror 留待 v1.4 再评估。
- **不采用** Electron/Tauri 全量桌面壳重写、PostgreSQL、微服务、完整 ORM 替换、SSR/Nuxt、在线 Office/WOPI 和直接内嵌 Microsoft Word。
- **退出主动工具链** Playwright 页面模拟；保留人工 UI 验收清单，自动验证只做 API、纯逻辑、类型、静态契约、构建和本地服务 HTTP 冒烟。

这不是保守，而是与产品定位相匹配：审迹是现场审计人员使用的本地离线效率工具，首要指标是数据可靠、启动稳定、旧项目可打开、交接可复核，不是技术栈看起来新。

### 1.1 是否升级的最终判断

| 范围 | 判断 | 优先级 | 原因 |
|---|---|---:|---|
| Python 运行时 | 应升级 | P1 | 3.11 已进入仅安全维护阶段，继续作为新版本基线会缩短维护窗口 |
| Node 构建运行时 | 应升级 | P0 消除漂移 / P1 升 24 | 22 系列处于 Maintenance LTS，且项目内 22.12/22.13 约束互相冲突；新版本宜转向支持窗口更长的 24 LTS |
| FastAPI/SQLite/Vue 核心选型 | 不换栈 | P0 决策 | 与本地离线、单项目单写、自包含交付高度匹配 |
| 后端代码结构 | 必须重构 | P0 | 数据库、接口、导出文件已达到 2,000～3,000 行级，v1.3 再堆功能会显著增加回归风险 |
| 前端代码结构 | 必须重构 | P0 | 根组件、操作面板和 API 客户端职责过重，驾驶舱、资料请求、复核、富文本会继续放大耦合 |
| 前端工程门禁 | 应补齐 | P1 | 当前有类型检查和构建，但缺少 lint、纯逻辑单测和接口类型同步 |
| 打包方式 | 保留并加固 | P1 | PyInstaller 已覆盖 Windows/macOS，换桌面壳没有直接业务收益 |
| 数据库引擎 | 保留 SQLite | P0 决策 | 当前单写者模型与项目文件夹自包含是核心产品能力，不应为“企业感”牺牲离线性 |

## 二、当前技术基线与问题证据

### 2.1 已确认技术栈

| 层级 | 当前状态 | 代码依据 |
|---|---|---|
| 后端运行时 | Python 3.11.11 | `scripts/build_macos.sh`、`scripts/build_windows.ps1` |
| Web/API | FastAPI 0.141.1、Uvicorn 0.52.1、Pydantic 2.13.4 | `requirements.txt` |
| 数据库 | Python `sqlite3`，自研 schema 迁移，schema v14 | `backend/database.py` |
| 前端 | Vue 3.5.22、TypeScript 5.9.3、Vite 7.1.7、Element Plus 2.11.8 | `frontend-v3/package.json` |
| 前端包管理 | pnpm 11.5.0 | `frontend-v3/package.json` |
| Node | package 声明 22.12.x，CI 使用 22.13.0，macOS 脚本要求 22.13+，Windows 脚本锁定 22.12.0 | package、CI、构建脚本 |
| 打包 | PyInstaller 6.22.0，Windows/macOS 原生构建 | `requirements-dev.txt`、`审迹.spec` |
| 后端质量门禁 | Ruff、pytest、哈希锁定依赖 | `pyproject.toml`、`requirements-dev.txt`、CI |
| 前端质量门禁 | vue-tsc + Vite build | `frontend-v3/package.json`、CI |
| 页面自动化 | Playwright 1.62.0 及一份页面用例 | package、`frontend-v3/e2e/` |

### 2.2 架构规模已经越过“继续堆在原文件里”的边界

静态行数如下：

| 文件 | 行数 | 主要风险 |
|---|---:|---|
| `backend/database.py` | 3,277 | schema、迁移、事务、仓储、业务规则混在同一个类中 |
| `backend/main.py` | 2,079 | 启动、会话、鉴权、项目状态和全部路由混在同一入口 |
| `backend/export.py` | 2,030 | 多种输出格式和模板细节集中，修改一种导出易波及其他格式 |
| `ProjectOperations.vue` | 1,257 | 低频管理操作形成巨型组件 |
| `frontend-v3/src/api/client.ts` | 895 | 类型、请求基础设施和所有业务 API 集中 |
| `App.vue` | 596 | 登录、主题、租约、项目、导航和页面编排集中 |

单个大文件不是原罪；但 v1.3 将新增驾驶舱、资料请求、复核意见、富文本、草稿、排序和导入预检。如果继续追加，风险会表现为：

1. 一个功能修改同时触碰会话、数据库、接口、列表刷新和导出。
2. 事务边界藏在大型方法中，难以确认“失败是否整批回滚”。
3. 新成员无法在短时间内判断改动影响范围。
4. 自动测试难以隔离业务规则，只能走越来越重的整链路测试。
5. 版本作用域、证据关联和审计日志更容易在不同页面出现口径漂移。

### 2.3 运行时版本存在现实升级压力

- Python 官方版本状态表显示，3.11 处于 security 状态，生命周期截至 2027-10；3.14 处于 bugfix 状态，生命周期截至 2030-10。对 2026 年开始的新开发基线，继续锁 3.11 不划算。[Python 官方版本状态](https://devguide.python.org/versions/)
- Node 官方计划显示，22 已处于 Maintenance LTS、生命周期截至 2027-04-30；24 的生命周期截至 2028-04-30。Node 在审迹中只承担构建职责，v1.3 转向 24 可获得更长维护窗口，升级影响面也较可控。[Node.js 官方发布计划](https://github.com/nodejs/Release)、[Node 22 到 24 迁移说明](https://nodejs.org/en/blog/migrations/v22-to-v24)
- Vite 7 要求 Node 20.19+ 或 22.12+。当前版本理论上满足最低要求，但项目内 22.12.x、22.13.0、22.13+ 三种规则不一致，已经构成不可复现风险。[Vite 7 迁移说明](https://v7.vite.dev/guide/migration)
- PyInstaller 当前支持 Python 3.8 及以上，且 6.15 已加入 Python 3.14 支持；因此 Python 升级不存在原则性阻断，但仍须在目标系统实际打包验证。[PyInstaller 手册](https://pyinstaller.org/en/stable/)、[PyInstaller 变更记录](https://pyinstaller.org/en/stable/CHANGES.html)

## 三、技术决策矩阵

### 3.1 保留：证明适合审迹的部分

| 技术 | 决策 | 论证 |
|---|---|---|
| FastAPI | 保留 | 本地 API、文件上传、类型模型和测试资产已成熟；瓶颈来自模块边界，不是框架性能 |
| SQLite | 保留 | 项目可整体拷走、无需安装数据库、事务可靠、单写者模型与现场使用一致 |
| Vue 3 + TypeScript | 保留 | 已满足 SPA 和复杂表单需求；Vue 官方对大型 SPA 推荐 Vue Router，并推荐 Pinia 作为新应用状态方案 |
| Element Plus | 保留 | 现有视觉和表单体系已建立；更换组件库会制造大量无业务价值的回归 |
| PyInstaller | 保留 | 能把 Python 与前端静态资源打成离线应用；官方明确要求在各目标系统分别构建，与当前 CI 路线一致 |
| 显式 SQL | 保留 | 审计数据迁移、约束和回滚需要清晰可见；不应把关键数据规则隐藏在 ORM 魔法中 |
| 哈希锁定 Python 依赖 + pnpm lock | 保留 | 可复现和供应链控制方向正确，只需统一声明与升级流程 |

### 3.2 升级：必须解决的底座问题

#### Python：3.11.11 → 3.14.x 标准构建

目标不是使用 3.14 新语法，而是获得更长的维护窗口。

实施约束：

- 使用标准 GIL 构建，不启用 free-threaded 模式，不把运行时实验特性带入 v1.3。
- 在兼容性分支先验证 FastAPI、Pydantic、openpyxl、SQLite backup、文件上传、PyInstaller、macOS arm64 和 Windows x64。
- 最终发布锁定到经验证的 3.14 补丁版本；不得只写 `3.14` 后在不同机器自动漂移。
- Ruff `target-version`、CI、Windows/macOS 构建脚本、开发文档和发布清单一次性对齐。
- 若 PyInstaller 或依赖在目标机出现 P0 阻断，允许临时退至 Python 3.13，但必须记录阻断证据和 3.14 回迁期限。

#### Node：22.x → 24 LTS

Node 不进入最终审计项目运行时，只用于构建前端，因此适合优先升级。

实施约束：

- 将 `package.json`、CI、`.node-version`、macOS/Windows 构建脚本统一到同一 24.x 补丁版本。
- `packageManager` 保留精确 pnpm 版本，通过 Corepack 启用，避免开发机全局 pnpm 漂移。
- 检查 Node 24 对 macOS 最低版本要求；如仍需支持早于 macOS 13.5 的开发构建机，CI 构建镜像需单独决策。
- 重建 lockfile 后核对差异，不接受“顺手升级全部前端包”的大范围变更。

#### 依赖声明：从“宽范围重新求解”改为“有计划地升级”

FastAPI 官方建议固定已验证版本，并在测试通过后再升级。当前 `requirements.in` 对 FastAPI 使用 `>=0.110,<1`，重新编译时跨度过宽。[FastAPI 版本管理说明](https://fastapi.tiangolo.com/deployment/versions/)

建议：

- 发布分支的直接依赖使用已验证的精确版本，或至少限定到当前 minor 范围。
- 直接使用的包必须写入输入清单；传递依赖不在输入清单中人为固定，最终锁文件仍保留完整精确版本和哈希。
- Python 与前端依赖按季度设升级窗口；业务开发期间只处理阻断性安全补丁。
- 每次升级只处理一个依赖簇，例如 FastAPI/Starlette/Pydantic 为一簇，Vue/Vite/插件为另一簇。

### 3.3 新增：为 v1.3 复杂度补齐的能力

| 能力 | 建议技术 | 使用边界 |
|---|---|---|
| 页面导航 | Vue Router，hash history | 支持驾驶舱、底稿、资料请求、复核和操作中心的可返回导航；避免要求 FastAPI 配置前端路由回退 |
| 全局状态 | Pinia | 只存登录会话、当前项目、导航筛选和任务状态；正式底稿内容仍以 SQLite 为准，不把敏感正文持久化到 localStorage |
| 富文本 | v1.4 候选 | v1.3 保持普通文本输入，不新增编辑器依赖或格式存储链路 |
| 拖动排序 | SortableJS 或轻量 Vue 绑定 | 仅提交稳定 UUID 顺序；后端事务重排；不改变编号、版本和附件归属 |
| 前端静态规范 | ESLint + typescript-eslint + eslint-plugin-vue | 先仅启用高价值规则，避免一次性格式化全库制造噪声 |
| 前端纯逻辑测试 | Vitest，`environment: node` | 只测状态机、排序、富文本转换、字数统计、API 映射；不挂载 DOM、不模拟页面 |
| API 类型同步 | FastAPI OpenAPI + openapi-typescript | 由 schema 生成 DTO 类型，继续保留手写业务 API 包装器；CI 检查生成结果是否漂移 |
| 依赖安全 | pip-audit、`pnpm audit --prod` | 仅在联网 CI/发布准备执行；例外必须记录版本、影响面、缓解措施和复核日期 |
| 发布物清单 | 现有 sha256 清单 + 依赖清单/SBOM | 便于确认现场安装包来源和依赖版本，不要求运行时联网 |

Vue 官方将 Vue Router 作为大多数 SPA 的推荐路由方案，并将 Pinia 作为新应用的推荐状态管理方案。[Vue 路由说明](https://vuejs.org/guide/scaling-up/routing)、[Vue 状态管理说明](https://vuejs.org/guide/scaling-up/state-management)

Tiptap 是基于 ProseMirror 的 headless 富文本框架，可按需组合扩展，并提供 Vue 适配和开源核心，符合审迹“只做所需能力”的边界。[Tiptap 官方说明](https://tiptap.dev/docs/editor/getting-started/overview)

### 3.4 暂不采用：看起来高级，但不适合 v1.3

| 候选技术 | 不采用原因 | 何时再评估 |
|---|---|---|
| Electron | 增加 Chromium 运行时、安装包和安全更新责任；现有本地服务架构已可用 | 必须深度接管系统窗口、托盘或设备能力时 |
| Tauri | 需要 Rust 和新的 IPC/打包体系，团队维护面扩大；不能直接解决审计业务问题 | 浏览器壳成为稳定性瓶颈且有明确原生需求时 |
| PostgreSQL | 破坏项目文件夹自包含和免安装；多人并发并非 v1.3 范围 | 正式进入局域网/云端多人协同时 |
| SQLAlchemy 全量 ORM | 迁移成本高，不能自动解决附件、快照和业务事务边界 | 新领域模型稳定后，可局部评估查询构造器，不做全量替换 |
| Alembic 直接接管迁移 | 不能替代当前迁移前项目快照、路径校验和文件系统联动 | 若未来采用 SQLAlchemy，再将其作为 DDL 工具评估 |
| 微服务 | 本地单进程工具没有独立部署和弹性扩容需求，只会增加故障点 | 云端多租户产品化时 |
| Nuxt/SSR | 审迹不是公开内容网站，不需要 SEO 或服务端页面渲染 | 当前无合理触发条件 |
| Microsoft Word/WOPI/OnlyOffice | 宿主、授权、服务端、联网和体积边界不符合离线轻量定位 | 企业客户明确采购授权并要求 Office 协同时 |
| GraphQL | 本地前后端同版本发布，REST + OpenAPI 已足够 | 资源聚合成为经测量的主要瓶颈时 |

## 四、目标架构：模块化单体，不做分布式

### 4.1 总体结构

```text
Vue 3 应用
  ├─ app：启动、路由、全局状态、会话和错误边界
  ├─ features：驾驶舱、底稿、证据、交流、资料请求、复核、项目操作
  └─ shared：API、通用组件、富文本、类型和工具函数
          │ HTTP / OpenAPI DTO
FastAPI 模块化单体
  ├─ api：请求校验、会话依赖、路由和错误映射
  ├─ services：用例、事务边界、权限和审计日志编排
  ├─ domain：状态机、值对象、业务错误和规则
  ├─ repositories：SQLite 显式 SQL 与数据映射
  ├─ db：连接、schema、版本迁移和项目快照
  └─ infra：文件、导出、任务、平台适配、日志和打包
          │
项目文件夹：audit.db + 附件库/ + 输出/ + 快照/
```

仍然是一个本地应用、一个服务进程、一个项目主连接和一个前端包。模块化的目的是让改动可隔离，不是把一个稳定进程拆成一堆不稳定服务。

### 4.2 后端建议目录

```text
backend/
  app.py                       # FastAPI 组装入口，只注册中间件和路由
  api/
    dependencies.py            # 会话、项目、operator 等依赖
    errors.py                  # 统一错误响应
    routes/
      session.py
      projects.py
      units.py
      issues.py
      evidence.py
      exchanges.py
      evidence_requests.py
      review_notes.py
      operations.py
  domain/
    issue.py                   # 状态、可编辑字段、版本规则
    evidence_request.py
    review_note.py
    errors.py
  services/
    issue_service.py
    ordering_service.py
    rich_text_service.py
    draft_service.py
    evidence_request_service.py
    review_service.py
    import_service.py
    archive_service.py
  repositories/
    unit_repository.py
    issue_repository.py
    evidence_repository.py
    exchange_repository.py
    request_repository.py
    review_repository.py
  db/
    connection.py
    schema.py
    migration_runner.py
    migrations/
      v15_rich_text.py
      v16_ordering_and_drafts.py
      v17_evidence_requests.py
      v18_review_notes.py
  infra/
    jobs.py
    file_store.py
    exporters/
    platform_adapter.py
    runtime_log.py
```

关键约束：

- 路由层不得直接拼 SQL，不负责跨表事务。
- 服务层定义事务范围，确保“业务记录 + 版本 + 审计日志”同成同败。
- 仓储层保留显式 SQL，不允许组件自行获取连接绕过锁和事务。
- domain 层不依赖 FastAPI，不依赖 Vue，不做文件系统操作。
- 自动任务继续使用项目级 FIFO；不能因模块化重新引入同项目并发写入。
- 迁移仍保留“先校验、先快照、后 DDL、失败拒绝打开”的安全模式。

### 4.3 前端建议目录

```text
frontend-v3/src/
  app/
    router.ts
    stores/
      session.ts
      project.ts
      navigation.ts
      jobs.ts
  views/
    ProjectListView.vue
    DashboardView.vue
    IssuesView.vue
    EvidenceRequestsView.vue
    ReviewNotesView.vue
    OperationsView.vue
  features/
    issues/
    rich-text/
    ordering/
    evidence/
    exchange/
    evidence-requests/
    review-notes/
    project-operations/
  shared/
    api/
      http.ts
      generated.ts
      session.ts
      projects.ts
      issues.ts
      evidence.ts
    components/
    types/
    utils/
```

状态边界：

- Pinia 管“应用状态”，SQLite 管“业务事实”。
- 富文本编辑中的瞬时选区、工具栏状态留在组件内。
- 当前项目、筛选条件和任务进度可进 store，但正式正文、证据内容和复核结论不得持久化到 localStorage。
- 草稿恢复写入项目数据库，继续服从项目单写租约和审计留痕边界。
- 路由采用 hash history，确保 PyInstaller 静态资源托管无需新增任意路径回退规则。

### 4.4 API 与错误契约

不建议为追求形式一次性把全部 `/api/...` 改成 `/api/v1/...`。前后端随同一安装包发布，强改路径只会制造迁移噪声。

v1.3 应先完成：

1. OpenAPI 生成前端 DTO 类型。
2. 统一错误结构：`code`、`message`、`details`、`request_id`。
3. 业务错误使用稳定 code，例如 `PROJECT_LEASE_LOST`、`ORDER_CONFLICT`、`SCHEMA_TOO_NEW`。
4. 新增 `/api/meta` 返回应用版本、schema 版本、功能能力和只读诊断信息。
5. 批量写接口全部具备预检或完整校验，避免逐项成功、逐项失败的半成品。

## 五、数据库与迁移重构

### 5.1 不换 SQLite，但必须拆迁移框架

当前 schema 初始化和 14 个版本演进集中在 `database.py`。v1.3 建议把迁移拆成独立、顺序执行的函数，每个版本包含：

- 适用前置版本。
- 数据预检 SQL。
- DDL/DML。
- 完成后校验 SQL。
- 是否需要文件系统联动。
- 失败提示和恢复说明。

### 5.2 建议 schema 顺序

| schema | 内容 | 独立发布要求 |
|---|---|---|
| v15 | 富文本 JSON、纯文本镜像和格式版本 | 先完成旧数据迁移与导出兼容 |
| v16 | 草稿、排序版本或重排审计字段 | 与富文本稳定后再合入 |
| v17 | 项目级资料请求及证据关联 | 独立事务和状态机测试 |
| v18 | 内部复核意见及版本/字段锚点 | 独立作用域一致性测试 |

不要在一个 migration 函数里一次创建所有 v1.3 表。分版本的目的不是多几个数字，而是让失败点和恢复范围可判断。

### 5.3 数据迁移门禁

- 迁移前用 SQLite backup API 创建一致性快照。
- 快照创建失败则拒绝迁移。
- 迁移前运行外键、重复 UUID、非法路径、孤儿附件和状态值预检。
- 数据迁移与 schema 写入同一事务；文件系统变更使用 staging 目录和最终交换。
- 迁移完成后执行 `foreign_key_check`、表约束抽查、关键计数对账和富文本纯文本镜像对账。
- 新版打开旧项目可迁移；旧版打开高 schema 项目必须拒绝，不能降级写入。
- 如需退回旧版，必须恢复迁移前快照，不能直接用旧程序打开新库。

## 六、分阶段实施路线

### 阶段 0：冻结基线与建立可回归样本（2～3 人日）

目标：先证明 v1.2 当前行为，避免重构时“凭印象保持兼容”。

交付：

- 固化一份脱敏的小、中、大项目样本和预期计数。
- 记录当前 API schema、导出字段、日志事件、数据库表/索引和项目目录清单。
- 跑通现有 pytest、Ruff、前端类型检查、生产构建和服务 HTTP 冒烟。
- 把人工 UI 场景写入清单，仅由用户执行。
- 冻结 v1.2 维护分支；v1.3 所有迁移以此为回退基线。

阻断条件：基线测试不能稳定复现、项目来源版本不明、发布产物无法重建。

### 阶段 1：统一并升级工具链（3～5 人日）

目标：只换运行时，不改业务、不改 schema。

顺序：

1. 先统一现有 Node 版本声明，消除 22.12/22.13 漂移。
2. 升 Node 24 LTS，重建前端锁文件并完成静态门禁。
3. 升 Python 3.14 标准构建，重编哈希锁文件。
4. 分别在 Windows x64、macOS arm64 构建 PyInstaller 安装包。
5. 用 v1.2 样本完成 API、导出、迁移、备份、归档和 HTTP 冒烟。

回滚：该阶段不改数据库，失败时直接恢复旧构建环境和锁文件。

### 阶段 2：先补工程门禁（2～4 人日）

目标：让后续拆分能快速发现行为漂移。

交付：

- 前端 ESLint。
- Vitest Node 纯逻辑测试。
- OpenAPI DTO 生成和漂移检查。
- 依赖审计、许可证/依赖清单和发布物校验。
- Playwright 从默认脚本、开发依赖和 CI 退出；其场景转写为人工验收条目。

明确禁止：jsdom、happy-dom、Vue Test Utils 组件挂载、Playwright、Cypress、页面截图和截图识别。

### 阶段 3：行为不变的后端拆分（7～10 人日）

采用“绞杀式”拆分，不新建第二套后端：

1. 先把路由从 `main.py` 分到 routers，仍调用原 `AuditProject`。
2. 把会话、项目租约、异常映射和启动器留在应用基础设施层。
3. 为新功能建立 service 接口，并让旧路由逐步调用 service。
4. 按单位、底稿、证据、交流、项目操作顺序抽取 repositories。
5. 最后拆 `export.py` 和迁移代码；这些与文件系统耦合最高，不能先动。

每拆一个域必须满足：API 响应兼容、数据库计数一致、日志事件一致、导出结果结构一致、全量后端测试通过。

### 阶段 4：行为不变的前端壳重构（5～7 人日）

顺序：

1. 建立 hash router，但先只映射现有项目列表和工作区。
2. 抽 session/project/navigation/jobs stores。
3. 把 `api/client.ts` 按域拆分，保持公共 `http.ts` 的会话头和错误处理只有一份。
4. 将 `ProjectOperations.vue` 按备份、归档、导入、导出、健康检查拆分。
5. 将 App 根组件降为应用编排，不承载具体业务。

回滚：不改 schema，不改 API 语义；出现问题可按功能域恢复旧组件。

### 阶段 5：建设 v1.3 功能底座（4～6 人日）

- 接入 Tiptap、SortableJS。
- 建立富文本 schema 白名单、JSON/纯文本转换器和字数统计。
- 建立排序、资料请求、复核意见状态机。
- 建立 schema v15～v18 迁移文件与迁移测试。
- 建立驾驶舱查询服务和统一作用域参数。

此阶段只交付技术底座和测试，不同时开放全部入口。

### 阶段 6：按成熟工具方案交付业务功能（28～38 人日）

按 `审迹v1.3_开发方案.md` 的 Must 顺序交付。建议每个功能完成后形成可安装内部版本，不等全部做完再第一次打包。

### 阶段 7：发布候选与现场验收（5～7 人日）

- 冻结依赖和 schema。
- 完成 Windows/macOS 构建、安装、旧项目迁移、崩溃恢复和备份恢复验证。
- 用户按人工 UI 清单验收，不由开发代理模拟页面。
- 对大项目样本执行性能门禁。
- 生成 sha256、版本清单、依赖清单、迁移说明和回退说明。

### 6.1 总体工作量

| 范围 | 估算 |
|---|---:|
| 纯技术升级与行为保持重构 | 19～29 人日 |
| v1.3 Must/Should 业务能力 | 28～38 人日 |
| 发布候选与人工验收修正 | 5～7 人日 |
| 合计 | 52～74 人日 |

单人开发建议按 12～16 周规划。若周期只能维持原来的 7～10 周，应减少 Should 功能或只完成一半结构重构，不能压缩迁移和数据安全测试。

## 七、测试与发布门禁

### 7.1 自动验证矩阵

| 层级 | 允许方式 | 核心范围 |
|---|---|---|
| Python 静态 | Ruff | import、基础错误、目标 Python 版本 |
| 后端单元 | pytest | 状态机、值对象、路径、安全规则 |
| 后端集成 | pytest + 临时 SQLite | 事务、迁移、版本、证据、日志、导入导出 |
| API 契约 | FastAPI TestClient/OpenAPI | 状态码、错误 code、DTO 漂移、会话作用域 |
| 前端静态 | vue-tsc、ESLint、Vite build | 类型、静态规则、生产构建 |
| 前端纯逻辑 | Vitest Node 环境 | 排序、转换、统计、状态机、API 映射 |
| 打包冒烟 | PyInstaller + HTTP 请求 | 进程启动、端点文件、首页资源 200、API健康状态 |
| 数据兼容 | 脱敏样本项目 | 旧库迁移、计数对账、回退快照、导出结构 |

不允许自动执行：浏览器控制、页面模拟、DOM 模拟、组件挂载、截图、截图比对、OCR 或其他截图识图。

### 7.2 发布 P0 阻断项

以下任一项失败，不发布：

- v1.2 项目无法迁移或迁移后计数/关联不一致。
- 迁移失败后没有可恢复快照。
- 富文本正文、纯文本镜像、搜索、版本、交流和导出任一链路不一致。
- 拖动排序改变 `seq`、`issue_code`、UUID、版本链或附件归属。
- 同一项目出现两个写者或同项目任务并发写入。
- 资料请求、复核意见的状态、列表、统计和正文锚点不在同一作用域。
- Windows/macOS 任一目标安装包无法启动或无法打开旧项目。
- 依赖存在已确认可利用且影响本地使用路径的高危漏洞，没有修复或书面缓解措施。
- 大项目基线较 v1.2 明显退化且无原因说明。
- 人工 UI 验收未由用户完成。

### 7.3 性能基线

至少覆盖：100 个被审单位、5,000 条底稿、20,000 个附件记录、2,000 条资料请求、2,000 条复核意见。

建议门槛：

- 项目摘要查询：P95 不超过 1 秒。
- 单位底稿列表：P95 不超过 300 毫秒。
- 全文搜索首屏：P95 不超过 1.5 秒。
- 单次排序提交：P95 不超过 500 毫秒。
- 普通富文本保存：P95 不超过 500 毫秒，不含大附件 I/O。
- 打包后冷启动：以 v1.2 实测为基线，退化超过 20% 必须解释并评估。

这些是后端/API 和纯逻辑性能测试，不需要模拟页面。

## 八、分支、提交与回滚策略

当前工作副本未检测到可用 Git 元数据。正式开工前必须先确认主仓库、v1.2 标签和发布分支；没有可回溯版本线，不应开始 schema v15。

建议里程碑：

- `v1.2-maintenance`：只接收 P0/P1 修复。
- `v1.3/toolchain`：仅运行时和锁文件。
- `v1.3/backend-modular`：行为不变的后端拆分。
- `v1.3/frontend-shell`：行为不变的前端拆分。
- `v1.3/features`：schema v15～v18 和业务功能。

每个提交遵守：

- 不把大规模改名、格式化和业务修改混在同一提交。
- 不把依赖大版本升级与 schema 迁移混在同一提交。
- 不把富文本接入与原普通文本字段删除混在同一版本。
- 每个 schema 版本有独立测试、样本和恢复说明。
- 发布候选只允许修复，不再升级工具链。

## 九、开工清单

### 9.1 第一周必须完成

- [ ] 确认代码仓库、v1.2 标签、构建产物和脱敏样本。
- [ ] 把现有自动门禁跑成绿色基线。
- [ ] 停用 Playwright 自动页面入口，转成人工验收清单。
- [ ] 新增 `.python-version`、`.node-version`，统一 CI 和双端脚本。
- [ ] 建 Python 3.14、Node 24 兼容分支，不改业务和 schema。
- [ ] 产出双端兼容矩阵和依赖锁差异报告。
- [ ] 补 OpenAPI 基线快照和数据库计数对账脚本。

### 9.2 开始功能开发前必须完成

- [ ] 后端路由与应用入口初步分离。
- [ ] 前端 router/store/http 基础设施就位。
- [ ] ESLint、Vitest Node、OpenAPI 类型漂移门禁就位。
- [ ] schema v15～v18 设计评审完成。
- [ ] 富文本 JSON 白名单、纯文本镜像和 Word 粘贴规范确定。
- [ ] 迁移、回滚和旧版拒绝高 schema 的自动测试通过。

## 十、验收后的技术状态

v1.3 重构完成后，应达到以下状态：

1. 核心技术栈仍然简单：一个 Vue 前端、一个 FastAPI 本地服务、一个 SQLite 项目库。
2. Python 与 Node 均处在可维护生命周期，所有环境声明一致。
3. 新业务按 features/services/repositories 归位，不再继续膨胀入口和单体类。
4. SQLite 自包含、单写租约、迁移快照、审计日志和附件路径安全全部保留。
5. 富文本、排序、资料请求、复核和驾驶舱使用同一业务规则与作用域。
6. 自动验证不依赖页面模拟，人工 UI 验收边界明确。
7. 任一 schema 升级都有迁移证据和可执行回退路径。
8. 技术升级不改变审迹的定位：现场更快、更稳、更少返工，而不是更像一套重型企业平台。

## 十一、最终技术选型清单

```text
运行时
  Python 3.14.x（标准 GIL 构建，锁定补丁版）
  Node.js 24.x LTS（仅构建，锁定补丁版）
  pnpm 11.x（锁定补丁版）

后端
  FastAPI + Pydantic + Uvicorn
  Python sqlite3 + 显式 SQL + 自研安全迁移 runner
  openpyxl
  PyInstaller

前端
  Vue 3 + TypeScript + Vite + Element Plus
  Vue Router（hash history）
  Pinia
  Tiptap/ProseMirror
  SortableJS

工程门禁
  Ruff + pytest
  vue-tsc + ESLint + Vite build
  Vitest（Node 纯逻辑，不使用 DOM）
  OpenAPI 类型生成与漂移检查
  pip-audit + pnpm audit --prod
  双端 PyInstaller 构建 + HTTP 冒烟 + sha256/依赖清单

明确不进入 v1.3
  Electron / Tauri / PostgreSQL / 微服务 / Nuxt SSR
  全量 SQLAlchemy/Alembic 替换
  Microsoft Word/WOPI/OnlyOffice 内嵌
  Playwright/Cypress/jsdom/截图识图自动化
```
