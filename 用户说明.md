# 审迹 1.1 用户说明

> 面向非技术用户，照做即可。全文中文。

## 一、安装

### macOS
1. 双击打开 `审迹.app`（dmg/zip 解压后）
2. 首次打开如提示"无法验证开发者"，右键 → 打开 → 确认
3. 程序没有窗口图标：双击后自动打开浏览器页面。程序运行中关掉浏览器后，**再次双击会重新打开浏览器页面**；退出程序请用页面内"项目菜单 ▾ → 退出程序"
4. 程序启动后浏览器自动打开，进入工作界面

### Windows
1. 解压下载的 zip（里面是一个 `审迹` 文件夹），整个文件夹放到任意位置（建议 `C:\审迹\`）
2. 双击文件夹里的 `审迹.exe`，浏览器自动打开
3. 注意：exe 旁的 `_internal\` 文件夹是运行必需，**整个文件夹一起拷贝/移动**，别只拷 exe（会闪退）

> 程序配置目录：`~/.shenji/`（单实例锁、崩溃日志）。使用人会话只保存在当前浏览器标签会话中；项目数据**全部在项目文件夹内**，与程序无关。

## 二、开始使用

1. **输入使用人姓名**（仅作操作留痕备注，不校验身份）
2. **创建/打开项目**：项目 = 一个文件夹，里面是 `audit.db` + `附件库/` + `输出/`。**新建项目目录会自动命名为 `项目名.auditproj` 并隐藏**（防误删改）：默认在文件管理器里看不到，用本机"最近项目"列表打开即可；打开已有项目不受影响
3. **建单位 → 建底稿 → 挂附件 → 导出**

### 核心流程（编制 → 复核 → 归档）
| 状态 | 含义 | 你能做什么 |
|---|---|---|
| 草稿 | 刚建立 | 编辑、提交复核 |
| 编制完成 | 已提交 | 复核通过 / 退回 |
| 复核退回 | 需修改 | 修改后重新提交 |
| 已复核 | 复核通过 | 归档 |
| 已归档 | 定稿 | 归档后编辑（需填原因，自动开新版本） |

- 复核退回时**必须填写退回意见**，意见留存在操作日志
- 已复核底稿被编辑会自动降回"编制完成"（改了就不会还显示已复核）
- 已归档底稿不能直接改，必须走"归档后编辑"并填写原因
- 新建底稿离开时若仍缺少“所属版块”或“缺陷定性”，程序会提示；确认后自动删除该未完成底稿
- 自动保存可选“输入停止后实时保存 / 每 5 分钟 / 每 20 分钟”，默认每 5 分钟；内容没有变化时不会增加版本

## 三、日常操作

| 功能 | 入口 | 说明 |
|---|---|---|
| 导出 Excel 台账 | 📄 导出 Excel | 含状态/版本数/证据提示列；带时间戳不覆盖旧文件 |
| 一键打包归档 | 📦 一键打包 | ZIP 内置归档清单（路径/大小/sha256） |
| 复制底稿字段 | 问题列表右上角「复制」 | 勾选字段后复制为表格文本，可直接粘贴到 Excel/Word |
| 编制与预设设置 | 项目菜单 → ⚙️ 编制与预设设置 | 选择自动保存模式；管理版块和问题分类预设 |
| 备份 | 项目菜单 → 💾 创建项目备份 | 仅生成一份 .auditbak 到项目上级目录，不再重复下载 |
| 恢复 | 菜单 → 恢复备份 | 从 .auditbak 恢复 |
| 合并导入 | 菜单 → 合并导入 | 汇总多个备份，保留底稿状态、版本链和证据关联，冲突自动报告 |
| 健康检查 | 菜单 → 🩺 项目健康检查 | 数据完整性 + 附件一致性 |
| 附件扫描 | 菜单 → 🔍 附件完整性扫描 | 全量哈希核对，可取消 |
| 汇总视图 | 菜单 → 📊 汇总视图 | 按状态/版块/单位三维汇总 |
| 删除项目 | 最近项目列表 → 删除 | 仅限程序创建的项目（`.auditproj`），二次确认后连数据库/附件/输出一并删除，不可恢复 |

## 四、数据安全（务必遵守）

1. **项目文件夹整体拷走 = 完整转移**（含 db + 附件 + 输出），程序不依赖外部数据
2. **输出不覆盖**：导出文件带时间戳后缀，放心多次导出
3. **定期备份**：重要项目每次收工前点「💾 创建项目备份」；备份文件只保存在项目**上级目录**
4. **删除操作会确认**：删除单位/底稿/附件都会弹确认框，点错可立即备份恢复
5. **附件文件夹按单文件规则处理**：一个文件夹 = 一个附件实体，不展开、不批量、不打包

## 五、升级

1. **先备份**所有打开的项目（项目菜单 → 💾 创建项目备份）
2. 下载新版本，替换旧程序（macOS 替换 .app，Windows 替换 .exe）
3. 旧项目文件夹**不用动**，直接打开即可（自动兼容旧数据）
4. 升级后如打不开项目，按提示操作（见"回滚"）

## 六、回滚

**原则：程序可以回退，项目数据永不丢。**

| 场景 | 做法 |
|---|---|
| 新版本打不开项目 | 保留旧版 .app/.exe 文件（升级前别删），用旧版打开即可 |
| 新版本写入后想回退 | 项目打开时若提示"更新版本创建"，先备份 .auditbak，用旧版继续 |
| 数据异常 | 用 .auditbak 恢复；恢复位置选空文件夹 |

> ⚠️ 注意：新版打开并编辑过的项目，旧版可能提示"更新版本创建"而拒绝打开——这是保护机制，防止旧版破坏新数据。此时**先备份**，再决定升级或继续用新版。

## 七、常见问题

| 现象 | 处理 |
|---|---|
| 启动提示"程序已在运行" | 已有一个实例，关掉重复启动的窗口 |
| 打开项目提示"更新版本创建" | 项目是用更新版程序建的，升级程序或按回滚策略处理 |
| 页面打不开 | 确认程序在运行；按启动提示中显示的本地地址访问 |
| 崩溃/异常 | 日志在 `~/.shenji/logs/crash_*.log`，可发给维护者排查 |
| 项目文件夹在文件管理器里看不到 | 新建项目自动隐藏（防误删改），用"最近项目"列表打开；确要在文件管理器里查看，开启"显示隐藏文件"（macOS Finder `Cmd+Shift+.` / Windows 查看→显示→隐藏的项目） |

---

*审迹 1.1 · 发布日期见发布产物清单*
