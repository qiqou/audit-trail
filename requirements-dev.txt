# 开发/CI 依赖（交付门禁，审查 F-08 修复）
pytest>=8.0
pygments>=2.15   # pytest 9.x terminalwriter 硬依赖（pip 不会自动带上，verify/CI 全量跑会炸）
ruff>=0.6
httpx>=0.27       # FastAPI TestClient 依赖
httpx2>=0.1       # starlette>=1.4 TestClient 改用 httpx2（新版必须，旧版 httpx 仅兼容告警）
