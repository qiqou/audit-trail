# 回滚策略（T12）

> 目标：程序可回退，项目数据永不丢。所有规则围绕"项目自包含 + 输出不覆盖 + 备份先行"三条铁律。

## 一、核心原则

1. **项目数据在项目文件夹内**（audit.db + 附件库/ + 输出/），与程序版本无关——回退程序不影响已存在的数据
2. **程序升级前保留旧版本**：macOS 旧 .app 存到 `~/审迹_旧版/`，Windows 旧 exe 存到 `C:\审迹_旧版\`
3. **升级/回退前先备份**（💾 备份 → .auditbak 到项目上级目录）

## 二、版本兼容检查（程序内保护）

程序打开项目时检查数据库 schema 版本（meta 表 `schema_version`）：

| 项目 schema 版本 | 程序行为 |
|---|---|
| 无版本号（v1.1 及更早） | 兼容，打开并写入当前版本号 |
| = 当前版本（v1） | 正常打开 |
| < 当前版本 | 兼容（未来扩展迁移路径） |
| **> 当前版本（更新版创建）** | **拒绝打开**，提示"请升级程序；如需回退请先备份" |

- 检查位置：打开项目时（`/api/project/open`）
- 保护目的：**防止旧版程序读取/写入新版数据结构导致数据损坏**
- 提示内容教用户怎么做（升级 / 备份 / 回退），不说"怎么了"

## 三、回滚操作步骤

### 场景 A：新版本有 bug，回退旧版
1. 旧版程序文件（升级前已保留）
2. 用旧版打开项目
3. 若提示"更新版本创建"：说明项目已被新版写入——**先备份**（若旧版打不开，用新版备份），
   然后二选一：
   - 继续用新版（等修复）
   - 从 `.auditbak` 恢复到未升级前的备份（恢复位置选空文件夹）

### 场景 B：项目数据异常（误删/损坏）
1. 找到最近一次 `.auditbak`（项目上级目录）
2. 菜单 → 恢复备份 → 选 .auditbak + 空目标文件夹
3. 恢复后自动打开恢复的项目

### 场景 C：升级后项目打不开（提示升级程序）
1. 确认用的是最新版
2. 项目 schema 版本 > 程序支持版本 → 用更新版程序打开
3. 若无法获取更新版 → 用旧版 .auditbak 备份恢复（前提：备份在升级前生成）

## 四、发布配套

- 每次发布生成 `manifest.txt`（版本号 + 文件大小 + sha256 树哈希），归档到发布目录
- 发布包保留最近 2 个版本（含 sha256 校验值），供回退使用
- 版本号单一来源：`backend/version.py`（spec / CI / 清单共用，杜绝漂移）

## 五、验证

- [x] 打开 v1.1 项目（无 schema_version）→ 兼容并写版本号（回归测试覆盖）
- [x] 打开 schema v2 项目（伪造）→ 拒绝 + 提示升级（测试覆盖）
- [x] 用户说明文档含回滚章节（本仓库 用户说明.md）
