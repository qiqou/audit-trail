# 审迹 v1.3 M3a 后端行为对账报告

日期：2026-08-21  
范围：后端模块化重构 D01～D08；不含双端候选包、目标机验证和人工 UI 验收。

## 结论

后端在不改变既有 API、项目 SQLite 事实源及高风险操作门禁的前提下完成本阶段模块边界拆分。自动对账通过；尚未把开发机结果当作 Windows/macOS 目标机发布验证。

## 模块边界

| 域 | 当前边界 | 保持不变的约束 |
|---|---|---|
| API | 按会话、项目、单位、底稿、附件、交流、运行期和操作域路由 | 会话、租约、错误码与 OpenAPI 契约 |
| 数据读取 | `repositories/units.py`、`issues.py`、`evidence.py`、`exchanges.py` | SQL 显式；写入不绕过项目锁 |
| 写入与日志 | `AuditProject` / service 层事务编排 | 业务更新与永久审计日志同事务 |
| 导出 | 确认单、操作日志、诊断支持包位于 `infra/exporters/` | 输出目录、唯一命名、日志字段、脱敏边界不变 |
| 数据库迁移 | 预检、SQLite backup 快照、换库后的受锁连接重建 | 预检失败不迁移；高版本数据库拒绝打开 |

## 自动对账证据

| 对账项 | 自动覆盖 |
|---|---|
| API / schema / 导出 / 审计事件契约 | `tests/test_contract_snapshot.py`、`tests/test_openapi_contract.py`、`tests/test_runtime_meta.py` |
| 单位、底稿、附件、交流读模型 | `test_unit_repository.py`、`test_issue_repository.py`、`test_evidence_repository.py`、`test_exchange_repository.py` |
| 原子导入、合并后重绑、预检令牌 | 导入与合并预检测试集 |
| 操作日志字段与诊断隐私 | `test_audit_log_export.py`、`test_diagnostics_support_package.py`、`test_operational_exporters.py` |
| v14～v17 升级、快照重开与高版本拒绝 | `test_jobs_and_migrations.py`、兼容测试集 |

本报告生成时，`ruff check backend tests` 通过，后端测试 **290 项通过**。前端关联门禁亦已通过：ESLint、Vitest Node（18 项）及 Vite 生产构建。

## 未在本阶段声称完成的事项

- Windows 10/11 与目标 macOS 的安装、启动和服务链验证（K06）。
- 真实界面下的拖动、主题、草稿恢复、DOCX Word 版式等人工 UI 验收（K07）。
- Excel 汇总与归档 ZIP 的进一步格式写入器拆分；该项不影响现有导出行为，但仍属于后续代码组织收口。
