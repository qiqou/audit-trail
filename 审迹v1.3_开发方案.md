# 审迹 v1.3 成熟工具开发方案

> 制定日期：2026-08-20  
> 基线版本：v1.2.0  
> 适用目录：`audit-trail-main/`  
> 方案版本：2.0（成熟工具版）  
> 验证边界：本方案仅基于分层静态扫描、只读检查和公开产品能力核对，不启动页面，不进行浏览器模拟、页面截图或截图识图。
> 配套技术方案：`审迹v1.3_技术栈升级与重构方案.md`
> 实施步骤计划：`审迹v1.3_实施开发步骤计划表.md`

> **范围调整（2026-08-20，优先于本文其余历史设计）**：v1.3 不开发富文本底稿与项目级资料请求台账。两项从 v1.3 Must、阶段门禁、验收和发布条件中剔除，移入 v1.4 候选池。现有富文本兼容字段及资料请求历史数据仅为打开既有项目而保留，不新增入口、API、迁移或工作流；本版本继续使用普通文本输入与既有交流记录能力。
>
> **范围调整（2026-08-22，发布候选前）**：v1.3 同时下线**内部复核意见闭环**与**问题确认单 DOCX 导出**，移入 v1.4 候选池。API 路由与前端入口移除，`/api/meta` 能力开关置 False；数据层、v18 迁移表（review_note_events）与写入器保留并经数据层测试覆盖。底稿复核状态流转（编制完成/复核退回/已复核/归档）属于 v1.2 既有核心流程，不受影响。

## 一、结论摘要

v1.3 不应只是“富文本 + 拖动排序”的界面升级，而应完成一次从“能记录底稿”到“能支撑现场审计日常运转”的产品升级。

本版本围绕现场审计人员一天内最频繁的五类动作建设：

1. 快速建立、复制、编写和排列底稿。
2. 发起、催收、核验和关闭资料请求。
3. 提出、回复、清除和重开内部复核意见。
4. 随时看清未完成底稿、待补资料、复核退回、备份和健康状态。
5. 将现场确认结果快速导出、交接、备份和归档。

因此，v1.3 建议形成八个 Must 模块：

- Word 风格富文本底稿。
- 单位和单位内底稿拖动排序。
- 项目现场驾驶舱与“我的待办”。
- 项目级资料请求台账。
- 独立的内部复核意见闭环。
- 项目内崩溃草稿恢复。
- Excel 导入预检。
- 大项目性能门禁。

另设置五个 Should 模块：底稿模板与快速复制、批量元数据维护、问题确认单 DOCX 导出、键盘快捷操作、诊断支持包。Should 功能不能挤占 Must 模块的数据安全和闭环测试时间。

关键技术决策：

- 不直接内置 Microsoft Word。当前产品是 Vue 3 + 本地 FastAPI 的浏览器工作台，Word 不是可直接嵌入的通用前端组件；Office Add-in 需要运行在 Word 宿主内，Office Online/WOPI 或 OnlyOffice 又会引入服务端、授权、体积和联网边界，不符合审迹“本地离线、自包含、轻量交付”的定位。
- 富文本编辑器及其 Tiptap/ProseMirror 技术路线移入 v1.4 候选池；v1.3 保持普通文本输入。
- 数据采用“富文本 JSON + 现有纯文本镜像”双轨存储。纯文本继续服务全文搜索、Excel 导出、复制、汇总和旧数据兼容；富文本 JSON 保存字体、字号、颜色、表格等结构。
- 拖动排序只更新 `sort_order`，绝不修改 `seq`、`issue_code`、UUID、版本链和附件关联。
- v1.3 不允许底稿跨单位拖动；跨单位移动会改变证据归属和审计范围，应另设带预检、确认和日志的专门功能。

成熟工具版建议开发量：约 28～38 个开发人日；单人开发约 7～10 周。若必须压缩周期，应先砍 Should，不得削弱迁移、草稿恢复、复核闭环和自动测试。

## 二、已加载的 v1.2 进度

### 2.1 当前状态

| 项目 | 已确认状态 | 依据 |
|---|---|---|
| 发布版本 | v1.2.0 | `backend/version.py`、`frontend-v3/src/version.ts`、`frontend-v3/package.json` |
| 数据库版本 | schema v14 | `backend/database.py` |
| 技术架构 | FastAPI + SQLite + Vue 3 + TypeScript + Element Plus | `README.md`、依赖清单 |
| 长文本编辑 | 三个 Element Plus 普通 `textarea` | `frontend-v3/src/components/IssueEditor.vue` |
| 单位排序 | `units.sort_order` 已存在，读取时已使用 | `backend/database.py:list_units()` |
| 底稿排序 | `issues.sort_order` 已存在，但主要列表仍按 `seq` 排序 | `backend/database.py:list_issues()`、`list_issues_by_unit()` |
| 重排接口 | 尚无单位/底稿重排 API | `backend/main.py`、`frontend-v3/src/api/client.ts` |
| 版本留痕 | 每次实际内容变化保存全字段 JSON 快照 | `backend/database.py:update_issue()` |
| 交流模式 | 修订、批注、待补资料、按轮次固化版本已实现 | `ExchangeWorkbench.vue`、`exchange_*` 表 |
| 数据安全 | 回收站、迁移快照、备份、合并预检、归档预检、单写租约已实现 | README、后端和测试集 |
| 自动测试资产 | 30 个测试文件，静态计数 214 个顶层测试函数 | `tests/` |

### 2.2 本次未完成的验证

当前工作副本没有项目虚拟环境或前端依赖目录，系统 Python 也未安装 pytest，因此本次未执行 pytest、前端类型检查和生产构建。这里记录的是“代码与文档基线已确认”，不是“v1.2 当前工作副本自动测试已通过”。

项目目录未检测到可用 Git 元数据，因此不能从本地提交历史判断最近改动；v1.3 开工前应先确认代码来源和版本标签。

## 三、产品定位与设计原则

### 3.1 一句话定位

审迹是“给现场审计人员使用的本地离线底稿与证据工作台”，核心价值是减少重复整理、避免资料和版本失控、让问题从发现到确认可追溯。

### 3.2 主要用户

| 用户 | 现场任务 | v1.3 必须解决的问题 |
|---|---|---|
| 项目组成员 | 收资料、写底稿、挂证据、回复复核 | 少录、少找、少重复、崩溃不丢稿 |
| 项目经理/主审 | 分工、看进度、复核、组织现场交流 | 一眼看到堵点，意见能闭环，不靠微信群催 |
| 复核人员 | 定位底稿、提出意见、检查修改 | 意见与底稿版本、字段、证据同一作用域 |
| 被审单位对接人 | 提供资料、确认问题、反馈整改安排 | 请求内容和状态清楚，现场沟通有完整留痕 |

### 3.3 明确不做什么

- 不做事务所级客户管理、收费、排期和人力资源系统。
- 不做企业级年度审计计划、审计宇宙、持续监控和 GRC 平台。
- 不在 v1.3 做云端多用户协同或局域网多人同时编辑；当前继续坚持单项目单写者。
- 不做完整 Microsoft Word 替代品，不承担复杂分页、宏、域、页眉页脚和任意 Office 插件兼容。
- 不在 v1.3 接入生成式 AI 自动写审计结论。底稿事实和审计判断必须由人员确认，AI 可另行立项并设置证据边界。
- 不内置通用数据分析平台。数据导入、抽样、关联分析属于后续独立模块，不能拖垮现场底稿主流程。

### 3.4 成熟工具的六条原则

1. **现场动作优先**：按审计人员每天实际点击次数排序，不按技术新鲜度排序。
2. **默认安全**：任何迁移、导入、批量修改、合并和归档都先预检，失败不产生半成品。
3. **一次录入，多处复用**：底稿正文、问题清单、交流稿、确认单和归档清单共用同一数据源。
4. **状态可解释**：每个状态都要告诉用户“现在是什么、谁要做什么、下一步去哪”。
5. **离线可完成**：没有互联网时，除外部分享外，全部现场核心流程可用。
6. **不靠记忆交接**：项目经理换人、电脑故障或隔天继续工作时，系统能恢复上下文。

## 四、成熟产品能力核对与取舍

公开产品资料显示，成熟审计工具通常覆盖计划、现场执行、证据、复核、问题、报告、跟进和审计轨迹；Caseware 还强调模板复用、PBC 资料请求、校验、复核意见、签署、历史与离线工作。审迹不需要照搬整套企业平台，但这些能力可以用作成熟度清单。

| 成熟能力 | 审迹 v1.2 | v1.3 取舍 |
|---|---|---|
| 现场底稿与证据 | 已有，基础较完整 | 继续加强富文本、快速复用和草稿恢复 |
| 资料请求/PBC | 仅存在于单条底稿交流会话 | 升级为项目级台账，仍与底稿和附件联动 |
| 内部复核意见 | 只有状态流转和交流批注，内部/外部语义混杂 | 新建独立复核意见模型和工作队列 |
| 项目进度 | 有汇总面板，但不是日常工作入口 | 新增现场驾驶舱和可跳转待办 |
| 模板和延续性 | 有版块/分类预设，无底稿模板 | 增加底稿模板、快速复制；跨年度结转暂缓 |
| 数据导入 | Excel 直接导入，缺少完整预检确认 | 增加只读预检、行级错误和一次性确认令牌 |
| 报告输出 | Excel 汇总和 ZIP 归档 | 增加现场问题确认单 DOCX，正式审计报告暂缓 |
| 离线/同步 | 离线和备份已有；无多人同步 | 保持离线，v1.3 不做多人同步 |
| 审计轨迹 | 日志链、版本、回收站已有 | 增加筛选导出、复核与请求事件覆盖 |

产品判断：大而全的软件不一定适合现场。审迹 v1.3 要做的是把最容易靠 Excel、微信群和脑子记的三件事——资料、复核、进度——收回到项目里。

## 五、现场工作流与当前缺口

### 5.1 目标工作流

```text
进入现场
  → 建项目/单位/成员与预设
  → 从模板或复制快速建底稿
  → 发资料请求并接收证据
  → 编制底稿、自动保草稿、形成版本
  → 内部复核、回复、清除或重开意见
  → 与被审单位交流修订并固化版本
  → 输出问题确认单和问题清单
  → 健康检查、备份、归档与交接
```

### 5.2 v1.2 的主要断点

- 打开项目后直接进入问题树，项目经理缺少“今天先处理什么”的入口。
- 待补资料存在于各交流会话中，无法按项目、单位、责任人、截止日统一查看和催收。
- 内部复核意见没有独立实体，容易与对被审单位的交流批注混淆。
- 新底稿只能从空白开始，重复问题仍要反复复制粘贴。
- 自动保存会直接产生正式版本；长时间编辑富文本后容易产生版本噪声，程序异常时又缺少独立草稿恢复。
- Excel 导入直接写入，缺少成熟工具应有的预检、确认和错误下载闭环。
- 汇总、搜索、日志、健康检查分散在项目菜单，不是连续的现场工作台。
- 无统一性能目标、诊断包和发布兼容矩阵，工程上仍像“能用的项目”，还不是“可长期维护的工具”。

## 六、v1.3 功能优先级

### 6.1 Must：缺一项不发布

| 编号 | 功能 | 现场价值 | 发布门禁 |
|---|---|---|---|
| M2 | 单位/底稿排序 | 项目树按现场工作顺序组织 | 顺序持久化，编号和证据链不变 |
| M3 | 项目现场驾驶舱 | 打开项目即看到堵点和下一步 | 所有数字可点击回到同一作用域明细 |
| M5 | 内部复核意见闭环 | 区分项目组复核与被审单位交流 | 提出、回复、清除、重开全留痕 |
| M6 | 项目内草稿恢复 | 断电/崩溃/误关闭不丢长文本 | 草稿不污染正式版本，可恢复或放弃 |
| M7 | Excel 导入预检 | 防止批量写入后再返工 | 预检只读、令牌确认、整批事务、错误报告 |
| M8 | 性能与大项目门禁 | 单位和底稿多时仍可现场使用 | 达到第 10.6 节规定的参考数据集目标 |

### 6.2 Should：数据底座稳定后交付

| 编号 | 功能 | 现场价值 |
|---|---|---|
| S1 | 底稿模板与快速复制 | 相似单位和重复问题不再从空白写起 |
| S2 | 批量元数据维护 | 批量修改版块、分类、编制人、复核人 |
| S3 | 问题确认单 DOCX 导出 | 现场会议可直接发 Word，不必二次排版 |
| S4 | 键盘快捷操作 | 高频用户减少鼠标来回移动 |
| S5 | 诊断支持包 | 报错时可取证，不必让用户描述“就是打不开” |

### 6.3 Could：有余量再做

- 单位/底稿收藏和最近访问。
- 项目内标签与自定义筛选视图。
- 只读交接摘要 PDF。
- 模板文件导入/导出和个人模板库。

### 6.4 明确延期

- 多人实时协同、云同步、移动端。
- OCR、AI 写作、自动定性、自动生成审计建议。
- 通用 SQL/Excel 数据分析引擎。
- 跨年度项目结转、事务所级资源排期、整改长期跟踪平台。

## 七、原始两项功能范围与边界

### 7.1 纳入范围

#### 富文本

- 适用字段：缺陷描述、制度依据、审计建议。
- 基础工具：字体、字号、文字颜色、加粗、斜体、下划线、清除格式。
- 段落工具：段落、标题、对齐、项目符号、编号列表、缩进、撤销、重做。
- 表格工具：插入表格、增删行列、合并/拆分单元格、删除表格。
- 统计：实时显示字数和字符数；字数口径固定为“中文字符 + 连续英文/数字词组”，字符数另显示非空白字符数。
- 粘贴：允许从 Word 粘贴基础文本样式和表格，清理脚本、外链样式、跟踪像素和不支持的标签。
- 只读显示：版本预览、交流正文、回收站预览均能安全渲染富文本。

#### 拖动排序

- 项目内被审计单位拖动排序。
- 单位内底稿拖动排序。
- 仅“按单位”视图启用；“按版块”视图是跨单位聚合，不提供拖动排序。
- 拖动后立即持久化；失败时前端恢复原顺序并提示原因。
- 同时提供“上移/下移”操作，作为键盘操作和拖动失败的替代入口。

### 7.2 本版本不纳入

- 不内置 Microsoft Word、Office Online、OnlyOffice 或 LibreOffice 窗口。
- 不实现 DOCX 原样导入、DOCX 排版级导出、页眉页脚、分页、批注气泡、修订模式复刻。
- 不把图片、附件或任意 Base64 内容直接写入富文本；审计证据继续通过附件系统管理。
- 不允许底稿跨单位拖动。
- 不改变底稿编号。拖动后编号可能呈现为非连续视觉顺序，这是“稳定编号”和“人工排序”并存的必然结果。

## 八、富文本技术方案

### 8.1 组件设计

新增通用组件：

```text
frontend-v3/src/components/rich-text/
├── RichTextEditor.vue      # 编辑器、工具栏、字数统计、禁用态
├── RichTextViewer.vue      # 版本/交流/回收站的只读安全渲染
├── richTextSchema.ts       # 允许的节点、标记、字体、字号、颜色规则
├── richTextNormalize.ts    # 规范化、纯文本提取、空内容判断
└── richTextPaste.ts        # Word 粘贴清洗与表格规范化
```

`IssueEditor.vue` 保留现有自动保存、未保存保护、状态机和归档限制，只将三个 `textarea` 替换为 `RichTextEditor`。组件通过 `v-model` 输出规范化 JSON，不自行调用 API，避免保存逻辑分散。

建议字体白名单：宋体、仿宋、黑体、楷体、Arial、Times New Roman；找不到字体时使用系统回退字体。建议字号白名单：9、10.5、12、14、16、18、22、26、36 pt。

### 8.2 数据模型

建议将 schema 从 v14 升至 v15，给 `issues` 增加：

```sql
defect_desc_rich       TEXT NOT NULL DEFAULT ''
regulation_basis_rich  TEXT NOT NULL DEFAULT ''
suggestion_rich        TEXT NOT NULL DEFAULT ''
rich_text_version      INTEGER NOT NULL DEFAULT 1
```

字段职责：

| 字段 | 作用 |
|---|---|
| 原 `defect_desc` 等字段 | 规范化纯文本镜像，用于搜索、复制、Excel、汇总和兼容 |
| `*_rich` | Tiptap/ProseMirror JSON 字符串，作为富文本格式的事实来源 |
| `rich_text_version` | 未来升级富文本 schema 时做兼容迁移 |

后端必须从富文本 JSON 自行提取纯文本，不能信任前端同时传来的纯文本镜像。这样可防止“页面显示一套、导出和搜索是另一套”。

写入限制建议：每个富文本字段纯文本不超过 100,000 字、JSON 不超过 2 MiB、表格最大 100 行 × 30 列。超过限制返回明确的 400 错误，不截断。

### 8.3 迁移与兼容

v14 → v15 迁移顺序：

1. 沿用现有机制，在 DDL 前创建 SQLite 一致性迁移快照。
2. 新增富文本列，不改写原纯文本列。
3. 对旧纯文本采用“懒转换”：读取时若 `*_rich` 为空，就在前端构造单段落 JSON；第一次保存时正式写入。
4. 迁移后执行数量、UUID、底稿字段、版本数和附件关联对账。
5. schema 升级后旧版程序应按现有高版本拒绝机制阻止打开，避免旧版覆盖富文本。

### 8.4 版本、交流与恢复

`ISSUE_FIELDS` 和版本快照必须同时包含纯文本与富文本字段。恢复历史纯文本版本时，自动生成基础段落 JSON；恢复 v1.3 版本时同时恢复格式与纯文本镜像。

交流修订不能继续只保存一个字符串，否则接受修订时会丢格式。建议给 `exchange_revisions` 增加：

```sql
old_rich_value TEXT NOT NULL DEFAULT ''
new_rich_value TEXT NOT NULL DEFAULT ''
value_format   TEXT NOT NULL DEFAULT 'plain'
```

- `old_value/new_value` 继续保存纯文本，供现有文字差异算法和修订摘要使用。
- 三个富文本字段的修订同时保存 `old_rich_value/new_rich_value`。
- 接受和“结束本轮”时，富文本 JSON 与纯文本镜像必须在同一事务中写入，并只固化一个版本。
- 交流批注 v1.3 仍维持字段级锚点，不扩展到段落内选区，避免范围失控。

### 8.5 安全与内容规范化

- 编辑器只允许白名单节点和属性；拒绝脚本、事件属性、iframe、任意 URL scheme 和内联图片。
- JSON 在后端做结构、深度、节点数、属性值和总大小校验。
- 只读渲染只消费已验证 JSON；如需生成 HTML，仍通过统一渲染器和前端消毒层处理，禁止直接把外部 HTML 交给 `v-html`。
- 规范化 JSON 后再比较保存签名，避免同一内容因无意义属性顺序变化不断生成新版本。
- Word 粘贴只保留白名单格式；不支持的样式退化为纯文本，不阻断录入。

### 8.6 受影响链路

| 链路 | v1.3 处理方式 |
|---|---|
| 全文搜索 | 继续搜索纯文本镜像，不搜索 JSON/HTML |
| Excel 导出 | 输出纯文本；表格按行换行、单元格用制表符分隔 |
| 复制到底稿/Word | 优先写入 `text/html` 与 `text/plain` 两种剪贴板格式；不支持时退回纯文本 |
| Excel 导入 | 导入为纯文本并生成基础段落 JSON |
| 版本预览 | `RichTextViewer` 只读渲染，摘要使用纯文本 |
| 回收站预览 | 与版本预览同一只读组件 |
| 交流模式 | 修订保存富文本 JSON，差异摘要仍基于纯文本 |
| 备份/归档 | SQLite 自包含，无需新增外部文件；清单和健康检查补充富文本结构校验 |
| 合并导入 | v1.3 来源保留富文本；v1.2 来源按纯文本生成基础 JSON |

## 九、拖动排序技术方案

### 9.1 排序语义

- 单位：按 `units.sort_order, units.id` 排序。
- 底稿：按 `issues.sort_order, issues.id` 排序。
- `seq` 和 `issue_code` 是稳定显示编号，只用于身份展示、冲突判断和编号复用，不再承担列表排序职责。
- 新建单位或底稿追加到当前同级列表末尾；建议 `sort_order` 使用连续整数并由整表重排接口统一归一化。

### 9.2 后端接口

新增请求模型：

```json
{ "ordered_ids": [12, 7, 31] }
```

新增接口：

```text
PUT /api/units/order
PUT /api/units/{unit_id}/issues/order
```

事务规则：

1. 请求必须包含当前范围内全部“未删除”实体 ID，且不得重复、缺失或夹带其他单位的底稿。
2. 校验通过后，在一个 SQLite 事务内更新全部 `sort_order`。
3. 写入一条审计日志，记录排序前后 ID/UUID 摘要、操作者和时间。
4. 排序不创建底稿内容版本，也不改变 `updated_at`；否则会把管理动作误认成底稿内容修订。
5. 接口执行前走现有项目写租约和会话校验；若后台破坏性任务占用项目，则返回明确冲突。

### 9.3 前端交互

- 在单位标题和底稿行左侧新增独立拖动手柄，避免点击选中、折叠和拖动相互冲突。
- 建议使用 SortableJS，锁定依赖版本并写入 `pnpm-lock.yaml`；只绑定列表，不引入整套页面设计系统。
- 单位折叠时仍可拖动单位；底稿仅在所属单位展开后拖动。
- 正在保存底稿内容时禁用底稿拖动；编辑器存在未保存内容时允许排序，但不得通过重新挂载编辑器丢失草稿。
- 放下后先乐观更新本地顺序，再调用 API；API 失败立即恢复原数组。
- “按版块”视图不显示拖动手柄，并明确提示“请切换到按单位视图排序”。
- 每个拖动手柄的更多菜单提供“上移/下移”，调用同一重排接口。

### 9.4 全局排序一致性

以下读取和导出必须统一改用 `sort_order`：

- `list_issues()`、`list_issues_by_unit()`。
- 项目汇总明细、问题清单、复制范围。
- Excel 台账、归档内汇总表。
- 交流模式左侧底稿导航。
- 合并后来源底稿的相对顺序：来源内部顺序保持，整体追加到目标同名单位末尾。
- 回收站恢复：优先恢复原 `sort_order`；若已冲突则追加到末尾，不改编号。

## 十、其他成熟能力详细设计

### 10.1 项目现场驾驶舱（M3）

打开项目后默认显示一个可关闭的现场驾驶舱，而不是另做一套管理系统。驾驶舱只展示能直接行动的数字：

- 今日/近 7 日有修改的底稿。
- 草稿、编制完成、复核退回、已复核、已归档数量。
- 未完成新底稿、未关联证据底稿、未关闭复核意见。
- 待提供、已逾期、待核验资料请求。
- 最近一次项目备份、恢复点、健康检查结果和未解决 P0/P1。
- 按单位和按人员的工作量分布，仅用于项目管理，不做绩效评价。

交互要求：每个数字必须可点击，并跳转到使用同一筛选口径的问题清单、资料请求或复核意见；不得出现“首页显示 5 条，点进去是 7 条”的作用域漂移。

建议接口：

```text
GET /api/project/dashboard
```

后端通过少量聚合 SQL 一次返回，不允许前端逐单位发 N+1 请求。驾驶舱结果只读，不缓存敏感正文。

### 10.2 项目级资料请求台账（M4）

现有 `exchange_requests` 只能表达“某条底稿交流中的待补资料”，不能覆盖进场时尚未形成问题底稿的通用资料清单。v1.3 应新增统一的 `evidence_requests`，允许项目级、单位级或底稿级请求；旧请求按原 UUID 迁入，并保留 `session_uuid` 与历史交流的关联。

核心字段：

```text
request_uuid    永久身份
unit_id         可选；单位级请求填写
issue_id        可选；底稿级请求填写
session_uuid    可选；从交流模式产生时填写
content         请求内容
status          open/provided/verified/withdrawn
priority        普通/紧急
due_date        计划提供日期
owner           项目组跟进人
requested_from  被审单位对接人或部门
provided_file_id 已提供资料
verified_by     核验人
verified_at     核验时间
```

`exchange_requests` 迁移后停止新增写入，仅保留兼容读取或在确认迁移完整后重建为兼容视图。交流模式和项目台账统一调用资料请求服务，避免两套状态互相打架。

项目级台账提供：

- 按单位、状态、截止日、跟进人、底稿筛选。
- 从台账新增请求至少选择请求层级；项目级请求可不选单位，单位级请求不强制选底稿。
- 上传资料后进入指定单位资料库；有关联底稿时同时关联底稿，继续沿用现有共享规则。
- “已提供”不等于“已核验”；核验通过必须由项目组人员确认。
- 撤回只做软删除式隐藏，永久历史仍在交流记录和审计日志中可见。
- 支持导出“资料请求清单.xlsx”，正文和状态来自同一数据源。

不得在 v1.3 建被审单位登录门户；当前仍由现场人员代录或导入反馈，避免伪装成有身份认证的协作系统。

### 10.3 内部复核意见闭环（M5）

内部复核意见与被审单位交流是两种业务，必须分表、分权限语义、分界面展示。新增 `review_notes`：

```sql
id               INTEGER PRIMARY KEY AUTOINCREMENT
note_uuid        TEXT UNIQUE NOT NULL
issue_id         INTEGER NOT NULL REFERENCES issues(id)
issue_uuid       TEXT NOT NULL
anchor_field     TEXT NOT NULL DEFAULT ''
body             TEXT NOT NULL
status           TEXT NOT NULL CHECK(status IN ('open','responded','cleared'))
assigned_to      TEXT NOT NULL DEFAULT ''
response         TEXT NOT NULL DEFAULT ''
raised_by        TEXT NOT NULL
raised_at        TEXT NOT NULL
responded_by     TEXT NOT NULL DEFAULT ''
responded_at     TEXT
cleared_by       TEXT NOT NULL DEFAULT ''
cleared_at       TEXT
reopened_count   INTEGER NOT NULL DEFAULT 0
```

同时新增不可变的 `review_note_events`，逐次记录 `raised/responded/cleared/reopened` 的操作者、时间、回复正文和当时绑定的底稿版本。`review_notes` 只保存当前状态和最新摘要，历史事件绝不覆盖。

状态规则：

```text
提出 open
  → 编制人回复 responded
  → 原提出人或复核人清除 cleared
  → 复核人可重开 open
```

- 编制人不能自行清除意见。
- 已归档底稿原则上不能新增普通复核意见；如发现归档缺陷，先走“归档后编辑”。
- 每个状态变化写审计日志，但不创建底稿内容版本。
- 复核意见绑定永久 `issue_uuid`，底稿进入回收站后仍可只读查看，恢复后继续有效。
- 字段级锚点必须与当前版本一起展示；正文版本切换时，意见数量、列表和定位使用同一作用域。

当前“使用人姓名”只是留痕备注，不是真实身份认证。v1.3 应增加项目成员表，将本机稳定 `account_id` 映射为项目负责人、编制人、复核人；清除复核意见按 `account_id` 和角色校验，不能只比较可随意填写的显示名。该机制仍是本地工作流控制，不应对外宣称为企业级身份认证；多人共用同一操作系统账户时应明确警告角色不可区分。

### 10.4 项目内草稿恢复（M6）

富文本上线后，继续把每次后台保存都当作正式版本，会造成版本爆炸；只靠 5/20 分钟自动保存，又不能接受现场断电丢稿。v1.3 将“工作草稿”和“正式版本”分开：

```sql
CREATE TABLE issue_drafts(
    issue_id       INTEGER PRIMARY KEY REFERENCES issues(id) ON DELETE CASCADE,
    issue_uuid     TEXT NOT NULL,
    draft_payload  TEXT NOT NULL,
    base_version   INTEGER,
    saved_by       TEXT NOT NULL,
    updated_at     TEXT NOT NULL
);
```

- 编辑停止约 8～10 秒后，将草稿写入项目数据库，不创建版本。
- 点击保存、到达正式自动保存周期、切换底稿确认保存时，草稿提交为正式内容和一个版本，然后删除草稿。
- 打开底稿时若草稿晚于正式版本，提示“恢复草稿 / 查看差异 / 放弃草稿”。
- 草稿跟项目一起备份，不写浏览器 LocalStorage、IndexedDB 或项目外临时目录，避免敏感正文散落。
- 草稿写入使用单行 UPSERT 和大小限制；程序异常不会留下半个 JSON。
- 归档、导出和交流固化前，必须先处理未提交草稿，不能静默忽略。

### 10.5 Excel 导入预检（M7）

现有直接导入改为两阶段：

```text
POST /api/import/excel/preflight
POST /api/import/excel/commit
```

预检必须只读，返回：

- 工作表、总行数、可导入数、跳过数、警告数、阻断数。
- 每行的单位、编号、字段校验结果和拟执行动作。
- 同名单位、重复底稿、非法金额、缺少必填字段、超长内容和公式型文本风险。
- 数据指纹和 10 分钟一次性 `confirmation_token`。
- 可下载的错误明细 Excel。

提交时重新核对文件摘要、项目 UUID、schema、当前数据指纹和令牌；整批在单事务中写入，任一阻断错误均不得留下部分单位或底稿。

### 10.6 性能与大项目门禁（M8）

参考验收数据集：100 个单位、5,000 条底稿、20,000 条附件记录、2,000 条资料请求、2,000 条复核意见，不要求加载真实附件正文。

在项目指定的发布参考电脑上，目标为：

| 操作 | 目标 |
|---|---:|
| 打开项目至可操作 | ≤ 5 秒 |
| 驾驶舱聚合 | ≤ 1.5 秒 |
| 全局搜索返回首屏 | ≤ 500 毫秒 |
| 切换普通底稿 | ≤ 300 毫秒 |
| 草稿保存/正式保存 | ≤ 1 秒 |
| 拖动排序提交 | ≤ 500 毫秒 |

实现要求：

- 问题树、资料台账和复核队列使用虚拟列表或分页，不一次渲染数千行。
- SQLite 为状态、单位、排序、截止日、未关闭意见补充复合索引。
- 聚合接口禁止 N+1 查询，并用查询计数测试守门。
- 富文本编辑器按需加载；只读列表不初始化编辑器实例。
- 性能测试只调用数据层/API 或纯函数，不启动模拟页面。

### 10.7 底稿模板与快速复制（S1）

第一阶段只做“问题底稿模板”，不做完整审计程序库：

- 模板字段：名称、适用版块、问题分类、缺陷定性、富文本描述骨架、制度依据、建议、默认风险级别。
- 从模板建底稿时产生新 UUID、新编号、草稿状态，模板本身不进入版本历史。
- “复制底稿”默认复制正文和分类，不复制金额、编制人、复核人和资料请求。
- 附件默认不复制；用户显式选择时只新增关联，不复制物理文件，并明确提示跨单位证据风险。
- 项目模板存入项目数据库。个人模板库作为 Could，通过导出/导入 `.audittemplate` 文件实现，不让项目依赖外部模板路径。

### 10.8 批量元数据维护（S2）

允许批量修改：所属版块、问题分类、编制人、复核人。禁止批量修改：正文、金额、状态、单位归属、底稿编号和附件关联。

批量操作必须先展示影响条数和前后值摘要，用户确认后整批事务执行；每条底稿不生成内容版本，只写一条批次日志和必要的实体关联明细。

### 10.9 问题确认单 DOCX 导出（S3）

为解决现场人员最终仍要回到 Word 排版的问题，v1.3 可增加模板化 DOCX 导出，而不是内置 Word：

- 范围：单条底稿、单个单位、勾选底稿。
- 内容：项目、单位、编号、问题分类、问题金额、富文本正文、制度依据、建议、被审单位反馈、附件索引和导出信息。
- 表格、字体、字号、颜色转换到受控 DOCX 样式；不承诺还原任意 Word 样式。
- 输出带时间戳不覆盖旧文件，写入操作日志。
- DOCX 结构自动检查与中文人工视觉验收分开报告。

### 10.10 键盘操作与快速导航（S4）

仅提供安全、可记忆的快捷键：

| 快捷键 | 动作 |
|---|---|
| Ctrl/Cmd + S | 保存当前底稿 |
| Ctrl/Cmd + K | 打开全局搜索 |
| Alt + ↑ / ↓ | 上一个/下一个底稿 |
| Ctrl/Cmd + Shift + N | 在当前单位新建底稿 |
| Esc | 关闭当前非破坏性弹窗 |

状态流转、删除、清空回收站、归档和批量提交不得设置单键快捷操作。

### 10.11 诊断支持包（S5）

新增“导出诊断信息”功能，默认只包含：

- 应用版本、schema 版本、操作系统、Python/前端构建版本。
- 最近崩溃日志和后台任务错误。
- 数据库完整性检查结果、表行数、索引清单、最近备份和健康检查状态。
- 文件路径只保留末级名称或散列；不包含底稿正文、附件内容和人员姓名。

用户可另行勾选“包含脱敏问题样例”，并在导出前预览清单。诊断包必须写入本地，不自动上传。

## 十一、数据与迁移架构

### 11.1 建议 schema 分段

不要把所有变化塞进一次不可定位的大迁移。建议按内部 schema 顺序交付：

| schema | 内容 |
|---|---|
| v15 | 富文本字段、格式版本、富文本修订扩展 |
| v16 | `issue_drafts`、排序索引和排序一致性修复 |
| v17 | `evidence_requests`、`review_notes`、`review_note_events`、`project_members` 及索引 |
| v18 | 底稿模板和批量操作批次记录 |

每一步迁移都必须幂等、可单独构造旧库测试；打开旧项目时仍只生成一次迁移前一致性快照，但 `schema_migrations` 逐步记录每个迁移结果。

### 11.2 新增核心数据约束

- 富文本 JSON 与纯文本镜像必须在同一事务生成和保存。
- 资料请求的 `provided_file_id` 必须指向未删除附件；核验状态必须有核验人和时间。
- 复核意见清除必须有清除人和时间；回复、清除和重开写不可变事件，不覆盖原记录。
- 项目角色绑定稳定 `account_id`，显示名只用于展示；同一账户不能通过改名绕过复核规则。
- 草稿的 `issue_uuid` 必须与目标底稿一致，避免 ID 重用或合并后误挂。
- 排序接口必须提交当前作用域完整 ID 集合。
- 模板和复制操作不得复用底稿 UUID、版本或审计日志身份。

### 11.3 统一作用域原则

驾驶舱数字、列表、正文标记、资料请求、复核意见和导出必须共用服务端筛选定义。禁止不同组件各自拼一套过滤条件。建议新增统一查询对象：

```text
unit_ids[] / issue_ids[] / statuses[] / departments[]
owners[] / due_state / review_state / evidence_state
updated_from / updated_to
```

前端只传筛选参数，计数和明细均由同一查询服务生成。

## 十二、工程成熟度要求

### 12.1 可靠性

- 所有批量写入必须事务化；文件系统与数据库共同变化时使用暂存目录、原子替换或补偿回滚。
- 归档、合并、导入、恢复继续走“只读预检 → 显式确认 → 执行前再核对 → 提交”。
- 程序异常退出后的草稿、后台任务和锁状态有明确恢复策略。
- 健康检查新增富文本结构、草稿悬挂、复核意见孤儿、请求附件失效和排序集合异常。

### 12.2 安全与保密

- 继续只监听 `127.0.0.1`；所有写接口要求有效会话和项目租约。
- 富文本、Excel、备份、模板和 DOCX 均视为不可信输入，限制大小、路径、公式、脚本和外部资源。
- 不新增远程遥测。性能和使用统计仅在本地即时计算，默认不持久化人员行为画像。
- 诊断包默认不含业务正文；任何可识别信息都需用户显式选择。
- 依赖新增前检查许可证、维护状态、包体增量和已知高危漏洞；依赖版本固定进锁文件。

### 12.3 可维护性

- 将 `ProjectOperations.vue` 拆分为搜索、汇总、备份、归档、回收站等低频面板，避免继续扩大单文件。
- 将后端聚合查询、富文本验证、复核、资料请求、模板拆为独立模块；`backend/database.py` 保留事务入口，不再无限增长。
- API 请求/响应使用明确 Pydantic 模型，不以任意 `dict` 扩散新字段。
- 新功能必须有错误码和可执行中文提示，不能只返回“操作失败”。

建议代码落点：

```text
backend/
├── services/
│   ├── rich_text.py
│   ├── drafts.py
│   ├── dashboard.py
│   ├── evidence_requests.py
│   ├── review_notes.py
│   ├── ordering.py
│   └── import_preflight.py
├── repositories/          # 聚合查询和事务内数据访问
└── api_models.py          # 明确请求/响应模型

frontend-v3/src/
├── components/rich-text/
├── components/dashboard/
├── components/evidence-requests/
├── components/review-notes/
├── components/templates/
└── features/              # 状态机、筛选、排序和快捷键纯函数
```

### 12.4 可用性

- 现场高频动作保持一至两层入口，低频破坏性操作继续收进项目菜单。
- 网络、磁盘、权限、文件损坏和版本不兼容提示必须说明下一步。
- 颜色不能作为唯一状态标识；同时使用文字、图标和数量。
- 破坏性确认展示具体影响范围，不使用笼统的“确定吗”。

### 12.5 发布与兼容

- 版本号统一更新为 1.3.0，后端、前端、spec、清单和用户说明由自动检查对照。
- 覆盖 v1.0/v1.1/v1.2 项目升级、v1.3 新项目、v1.2 备份导入 v1.3、v1.3 项目被旧版拒绝四条矩阵。
- Windows 10/11 与当前支持的 macOS 分别打包和人工启动验收。
- 发布包提供 sha256、迁移说明、已知限制和明确回滚步骤。

## 十三、实施路线与工作量

| 阶段 | 主要工作 | 预计人日 | 交付门禁 |
|---|---|---:|---|
| 0. 基线冻结 | 确认代码来源；建立可复现 Python/Node 环境；跑现有 lint、pytest、typecheck、build | 1～2 | v1.2 自动基线有记录；失败项先定责 |
| 1. 数据与模块拆分 | schema v15～v18 框架；拆分数据库/前端巨型组件；统一查询作用域 | 3～4 | 迁移可单独测试；v1.2 行为无回归 |
| 2. v1.3a 编制安全 | 富文本、草稿恢复、版本/交流适配、排序 | 8～10 | 富文本和排序 P0 全过；旧项目无损升级 |
| 3. v1.3b 现场闭环 | 驾驶舱、资料台账、内部复核意见 | 7～9 | 数字与明细同口径；请求和意见全流程可追溯 |
| 4. v1.3c 效率增强 | 导入预检、模板/复制、批量元数据、快捷键 | 4～6 | 批量操作均预检且整批事务；复制不串 UUID/证据 |
| 5. 输出与支持 | DOCX 确认单、诊断包、日志筛选导出 | 2～3 | 输出不覆盖；诊断默认不含正文 |
| 6. 性能与发布 | 大项目基准、全量回归、双平台打包、文档和清单 | 3～4 | 性能门禁和自动门禁通过；人工验收清单移交 |

### 13.1 交付节奏

- `v1.3a` 先解决“写得快且不丢”：富文本、草稿、排序和迁移。
- `v1.3b` 再解决“项目经理看得清”：驾驶舱、资料和复核闭环。
- `v1.3c` 最后解决“重复活少干”：模板、复制、批量处理、导出和诊断。
- 三个阶段均在同一 v1.3 分支推进，但每阶段有独立迁移测试、回归报告和可回退提交点。

### 13.2 开工顺序

第一批代码不得直接从富文本工具栏开始。正确顺序是：

1. 恢复可复现环境并冻结 v1.2 自动基线。
2. 写 schema v15～v18 的迁移测试和旧库样本。
3. 抽离统一查询作用域、富文本规范化和草稿事务接口。
4. 再实现前端组件和交互。

建议保持“小步提交”：一次提交只处理一类 schema、一个后端能力或一个前端模块，避免迁移、富文本和复核功能混在同一大提交中。

## 十四、自动测试与验收标准

### 14.1 自动测试（允许执行）

本项目约束下只进行代码级和服务级自动验证，不启动浏览器，不运行 Playwright/Cypress，不生成截图，不做截图识别。

#### 后端新增测试

- v14 → v15 迁移：至少构造 1,000 条底稿，对账纯文本、富文本生成、UUID、版本数、附件关联和日志链。
- 富文本 JSON 白名单、超长、超深、超大表格、恶意 URL、脚本属性拒绝测试。
- 后端纯文本提取与前端测试向量一致。
- 更新、自动保存、已复核降级、已归档拒绝、版本恢复测试。
- 交流修订接受/拒绝/结束本轮测试，确认格式和纯文本在同一版本固化。
- Excel 导入/导出、复制数据源、搜索、回收站、备份恢复、v1.2/v1.3 合并兼容测试。
- 单位/底稿排序成功、重复 ID、缺失 ID、越权 ID、跨单位 ID、事务回滚、日志留痕测试。
- 新建、删除、恢复、合并后 `sort_order` 冲突处理测试。
- 草稿 UPSERT、正式提交、恢复、放弃、基线版本冲突、归档前阻断测试。
- 旧 `exchange_requests` 向统一资料请求迁移，以及按项目/单位/状态/截止日筛选、提供附件、核验、撤回和孤儿关联测试。
- 复核意见提出、回复、清除、重开、不可变事件、回收站只读和基于 `account_id` 的角色语义测试。
- 驾驶舱计数与对应明细使用同一查询作用域的契约测试。
- Excel 导入预检不写数据、令牌过期/指纹变化拒绝、整批回滚测试。
- 模板和复制生成新 UUID、新编号、草稿状态且不默认复制附件的测试。
- 诊断包默认不含正文、人员姓名和完整路径的隐私测试。
- 参考大项目的数据层/API 性能基准与查询次数上限测试。

#### 前端静态与组件逻辑测试

- TypeScript 类型检查和生产构建。
- 富文本规范化、纯文本提取、字数统计、Word 粘贴清洗使用纯函数测试。
- 排序数组变换、API 失败回滚、按版块视图禁用规则使用纯函数测试。
- 驾驶舱筛选参数、资料请求状态机、复核意见状态机和快捷键映射使用纯函数测试。
- 保留静态模板契约检查，但不得通过启动页面、挂载模拟 DOM、截图或截图识别完成。

### 14.2 人工 UI 验收（由用户执行）

自动验证通过后，提供逐项人工验收清单，但不代替用户操作：

1. 中文字体、字号、颜色、加粗、列表、对齐、表格操作是否符合预期。
2. 从实际 Word 文档粘贴文字和表格后的视觉效果。
3. 三种主题、窄屏、长表格和工具栏换行体验。
4. 连续输入、切换底稿、自动保存、断开服务后的未保存保护。
5. 单位和底稿拖动手感、折叠状态、失败提示和顺序恢复。
6. 重启程序、备份恢复、合并导入后顺序与富文本是否保持。
7. 驾驶舱数字点击后，明细是否与数字完全一致。
8. 资料请求从新增、提供、核验到撤回的入口是否清楚。
9. 编制人回复复核意见后，是否只能由复核人清除和重开。
10. 程序异常退出后，草稿恢复是否能看懂且不会覆盖正式版本。
11. 5,000 条底稿数据集下，搜索、切换、筛选和拖动是否可接受。
12. DOCX 在用户实际 Word 环境中的中文字体、表格和分页效果。

人工验收结果必须与自动测试结果分开报告。

## 十五、发布阻断项（P0）

出现以下任一情况不得发布 v1.3：

- v1.2 项目迁移后纯文本、版本、附件关联、交流历史或日志链有任何丢失。
- 富文本内容与纯文本镜像不一致，导致页面、搜索、导出结果不一致。
- 版本恢复或交流修订会丢字体、表格或回退成旧内容。
- 富文本可写入脚本、任意外链资源、内联图片或超限内容。
- 拖动排序改变底稿编号、状态、版本数、附件关联或更新时间。
- 排序请求部分成功，造成重复/缺失 `sort_order`，或者跨单位底稿被静默移动。
- Excel、归档、备份或合并输出出现富文本 JSON/HTML 原文，或 v1.2 来源无法兼容导入。
- 草稿恢复覆盖了更新的正式版本，或草稿被写到项目文件夹外。
- 驾驶舱、资料台账、复核队列的计数与明细作用域不一致。
- 资料请求“已核验”但没有有效附件、核验人或核验时间。
- 编制人可自行清除复核意见、可通过修改显示名绕过角色规则，或清除/重开历史被覆盖。
- Excel 导入未预检即可写入，或提交失败后留下部分数据。
- 大项目基准明显超出性能目标且没有明确降级提示。
- Python 回归测试、前端类型检查或生产构建未通过。

## 十六、主要风险与控制

| 风险 | 等级 | 控制措施 |
|---|---|---|
| 直接把 HTML 存进旧字段，污染搜索和导出 | P0 | 富文本 JSON + 后端生成纯文本镜像 |
| Word 粘贴带入脚本、图片或远程资源 | P0 | 前后端双层白名单；拒绝图片和危险 URL |
| 富文本 JSON 的无意义差异导致版本爆炸 | P1 | 保存前规范化；按规范化 JSON 计算签名 |
| 交流修订只改纯文本导致格式丢失 | P0 | 修订同时保存 plain/rich，并在同一事务固化 |
| 排序与编号耦合，破坏稳定编号 | P0 | 排序只写 `sort_order`，所有编号字段只读 |
| 部门视图拖动产生含糊归属 | P1 | 仅按单位视图允许排序 |
| 引入完整 Office 套件导致包体和部署失控 | P1 | 采用轻量编辑器内核，只装必要扩展 |
| 驾驶舱成为新的“看板孤岛” | P0 | 所有数字和明细共用服务端查询对象 |
| 内部复核和被审单位交流再次混用 | P0 | 独立表、独立状态机、独立入口和日志动作 |
| 草稿长期堆积或错误覆盖正式版本 | P0 | 基线版本校验、过期提示、归档前处理和健康检查 |
| 资料请求增加字段后操作过重 | P1 | 新增时只要求内容和底稿，其余字段按需补充 |
| 功能过多导致 v1.3 无法收口 | P1 | Must/Should 分层，Should 不阻塞核心发布 |
| DOCX 输出被误认为完整 Word 兼容 | P1 | 固定模板和支持范围，明确不还原任意 Office 排版 |
| 当前无可复现开发环境，无法确认 v1.2 基线 | P0 | 阶段 0 先恢复依赖并记录完整门禁结果 |

## 十七、建议的 v1.3 完成定义

只有同时满足以下条件，才能称为 v1.3 开发完成：

- 三个长文本字段可稳定富文本编辑、只读显示、版本恢复和交流修订。
- 搜索、复制和 Excel 仍得到干净纯文本，备份/合并保留完整格式。
- 单位和单位内底稿排序可持久化，编号和审计证据链不变。
- 打开项目可直接看到可行动的现场驾驶舱，数字与明细完全一致。
- 资料请求可按项目统一查看，提供、核验、撤回和附件关联完整留痕。
- 内部复核意见与被审单位交流彻底分开，提出、回复、清除和重开形成闭环。
- 程序异常退出后可恢复项目内草稿，且不污染正式版本历史。
- Excel 导入必须经过只读预检和一次性确认，失败不产生部分数据。
- 参考大项目达到约定性能目标，或对超大范围明确分页和降级。
- v1.2 项目迁移经过数据对账和回滚快照验证。
- 自动测试、静态检查、前端构建和双平台构建门禁通过。
- 已向用户移交人工 UI 验收清单，并明确标注人工验收是否通过。

## 十八、公开能力参考

这些资料只用于核对成熟审计工具的能力边界，不代表审迹照搬其云端、企业级或商业模式：

- [Wolters Kluwer TeamMate+ Audit](https://www.wolterskluwer.com/en/solutions/teammate/teammate-audit)：覆盖计划、现场执行、报告、关闭和跟进的端到端审计工作流。
- [Wolters Kluwer TeamMate Internal Audit Management](https://www.wolterskluwer.com/en/solutions/teammate/internal-audit-management-software)：强调计划与排期、角色视图、资料请求、问题跟踪、报告和审计轨迹。
- [Caseware Working Papers](https://www.caseware.com/uk/products/working-papers)：强调桌面项目文件、模板、PBC 请求、内置校验、工作底稿关联和延续使用。
- [Caseware Review Notes](https://documentation.caseware.com/latest/Audit/en_us/Content/User_Completing_and_Reporting/Issue_Notes_Review_Notes/c_Overview.htm)：复核意见可关联具体底稿，并区分未清除意见。
- [Caseware SmartSync](https://documentation.caseware.com/2023/WorkingPapers/en/Content/Setup/Environments-Configuration/CaseWare-SmartSync/SS-Getting-Started-Guide.htm)：说明现场离线工作、恢复连接和冲突处理的重要性；审迹 v1.3 只借鉴离线可靠性，不实现同步。
