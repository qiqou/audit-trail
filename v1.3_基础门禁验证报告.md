# v1.3 基础门禁验证报告

执行日期：2026-08-20。验证仅运行代码、类型、构建、服务和依赖检查；未启动或控制页面，未使用截图或识图。

| 检查 | 结果 |
|---|---|
| 运行时版本矩阵 | 通过：Python 3.14.6 / Node 24.19.0 / pnpm 11.5.0 声明一致 |
| Python 哈希锁 | 通过：`pip install --dry-run --require-hashes -r requirements-dev.txt` |
| Ruff | 通过：无错误 |
| 后端回归 | 通过：259 项，1 项已有弃用警告 |
| OpenAPI 前端 DTO 漂移 | 通过 |
| ESLint | 通过：零警告 |
| Vitest Node 纯逻辑 | 通过：4 文件、11 断言 |
| 前端生产构建 | 通过；仅有上游 Rollup `PURE` 注释警告 |
| Python 依赖审计 | 通过：无已知漏洞 |
| 前端生产依赖审计 | 通过：无已知漏洞 |

## 未完成的发布环境验证

当前机器为 macOS 27、Node 26.5.0，不是锁定的 macOS 14、Node 24.19.0 发布环境；也不具备 Windows x64 环境。因此 Windows x64 与 macOS 14 arm64 的 PyInstaller 产物和 HTTP 冒烟仍需由 CI 或对应发布机执行。该限制已保留在计划 B06，未以本机结果替代。

## 自动化边界

已删除 Playwright 配置和页面用例。自动化门禁只保留静态检查、纯逻辑单元测试、OpenAPI/SQLite/导出/审计事件契约、后端 API 测试、依赖审计和 Vite 构建；界面交互转入 `v1.3_人工UI验收清单.md` 由人工执行。
