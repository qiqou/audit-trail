# 审迹 v1.3 供应链与版本检查报告

> 执行时间：2026-08-21。

- 版本单一来源已统一为 `1.3.0`：后端 `backend/version.py`、前端 `package.json` 与展示版本一致。
- 运行时矩阵检查通过：Python 3.14.6、Node 24.19.0、pnpm 11.5.0。
- `pip-audit -r requirements.txt`：未发现已知漏洞。
- `pnpm audit --prod --registry=https://registry.npmjs.org`：未发现已知漏洞。
- 默认镜像站不实现 pnpm 安全审计接口；发布检查应明确使用官方 registry 审计端点。当前开发机 Node 26.5.0 与目标锁定 Node 24.19.0 不同，双端发布构建必须在目标运行时重新验证。
