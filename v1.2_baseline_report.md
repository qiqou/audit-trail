# v1.2.0 自动基线报告

> 执行日期：2026-08-20
> 基线：Git 标签 `v1.2.0` / 提交 `8e7c5586658ccd92f3ef893de000995a3d271edc`
> 执行位置：隔离 Git worktree；未启动或控制页面、未执行截图或识图。

## 结果

| 检查 | 结果 | 说明 |
|---|---|---|
| `ruff check .` | 通过 | 无静态检查错误。 |
| `vue-tsc --noEmit` | 通过 | 使用锁定依赖执行。 |
| `vite build` | 通过 | 仅有上游 Rollup `PURE` 注释警告。 |
| `pytest tests -q` | 231 通过 | 1 个 Starlette/httpx 弃用警告。 |
| HTTP 冒烟 | 通过 | 隔离端口根路径返回 HTTP 200。 |
| 原 `pnpm build` | 未通过 | 脚本调用标签中不存在的 `scripts/check_frontend_ui_contract.mjs`；这是发布标签的可复现构建缺陷，未被静默绕过。 |

## 运行时记录

- Python：3.14.6。
- Node：本地执行环境为 26.5.0；标签声明为 22.12.x，因此命令给出引擎警告但未造成类型、构建或测试失败。
- pnpm：11.5.0。

## 结论

v1.2 的运行时功能基线可复现：后端回归与静态资源服务均通过。v1.3 发布前应修复或从正式构建脚本中移除失效的 UI 契约脚本引用，并在 Node 24 目标环境重新完成完整门禁。
