# 审迹 v1.3 兼容矩阵报告

> 执行时间：2026-08-21；范围为本机自动化验证，不替代双端发布候选和人工 UI 验收。

| 场景 | 自动验证 | 结果 |
|---|---|---|
| 早期项目（无 schema_version）打开 | `test_version_compat.py` | 通过：写入当前 schema，并保留可打开性 |
| v1/v2/v6 历史 schema 升级 | `test_jobs_and_migrations.py` | 通过：DDL 前创建快照，关键字段/计数保持 |
| v1.1 真实物理表序升级 | `test_jobs_and_migrations.py` | 通过：字段不按 `SELECT *` 错位，交流外键有效 |
| v17→v18 草稿/复核迁移 | `test_issue_drafts.py`、`test_migration_preflight.py` | 通过：预检、快照与新表约束有效 |
| 更新 schema 拒绝 | `test_version_compat.py` | 通过：提示升级和备份，不允许旧程序写入 |
| 备份、归档、合并预检 | `test_archive_preflight.py`、`test_merge_preflight.py` | 通过：预检/确认/再核对门禁保持 |
| 输出兼容 | `test_confirmation_docx.py` | 通过：确认单 DOCX 包结构、字段与下载路径有效 |

本轮共执行 28 项兼容相关自动测试，全部通过。真实 Windows/macOS 打包后的打开、恢复与归档服务链留至 K06。
